#ifndef CORE_USING_ALREADYINCLUDED
#define CORE_USING_ALREADYINCLUDED

#include <cstdio>
#include <cstdlib>

namespace core {

// Since the namespace core is often accessed via using directive, we want to
// keep it as free of clutter as possible, so we don't bring anything else in.

}

#endif
